We use vite tool as a bundler to make ract projects

// we use create app previously

//now we use vite 
folder structure used by vite
main is src

rafce is used to give bioler plate or make components

u cant retur a single elemnt u can use a elemnt inside it it has multiple elemnts
 or use fragmentation<></>


exclint when collaborative coding used to make custom rules
oxclint

gitignor used to ignore code which is not used to push to git hub

package .json dependencies rhti hai
vite config 